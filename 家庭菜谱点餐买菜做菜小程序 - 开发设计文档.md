# 家庭菜谱点餐买菜做菜小程序 \- 开发设计文档

# 一、文档说明

## 1\.1 文档目的

本文档为微信小程序“饭桶宝”的开发设计文档，明确小程序的功能模块、数据结构、接口设计、页面交互、业务流程等全部开发细节，供AI开发员工直接使用，无需额外沟通，即可完成全流程开发，确保开发成果完全匹配用户需求。

## 1\.2 开发环境与技术栈

- 开发环境：微信开发者工具（最新稳定版）

- 技术栈：微信原生小程序（WXML、WXSS、JavaScript）、云开发（云数据库、云函数、云存储）

- AI能力：集成小红书链接解析AI接口（需支持提取链接内菜谱名称、食材、调味料及用量、备菜步骤、做菜步骤）、AI图片生成接口（支持根据菜名生成菜品图片）

- 兼容要求：兼容微信版本8\.0及以上，适配手机端所有主流尺寸（375px\-414px宽度）

## 1\.3 核心需求回顾

1\. 多用户可加入多个家庭，支持家庭切换，可查看当前家庭成员；2\. 支持添加小红书链接，AI提炼菜谱核心信息（菜名、食材、调料及用量、备菜/做菜步骤），支持用户手动修改，每个菜谱需配备图片（AI生成或用户上传，可后续修改）；3\. 家庭成员可点菜，可添加备注信息，生成待买菜点菜单；4\. 做饭成员可查看买菜清单（checklist），完成后点菜单变为待制作；5\. 做饭成员可查看做菜清单（备菜\+做菜步骤，checklist），完成后点菜单闭环。

# 二、整体架构设计

## 2\.1 架构分层

1. 表现层（页面）：负责用户交互，展示所有功能模块（家庭管理、菜谱管理、点餐、买菜、做菜等），新增家庭切换、菜谱图片上传/AI生成、点菜备注等交互；

2. 业务逻辑层（云函数\+页面逻辑）：处理核心业务流程（家庭创建/加入/切换、菜谱解析/修改/图片处理、点餐/买菜/做菜状态流转、点菜备注管理等）；

3. 数据层（云数据库\+云存储）：存储用户、家庭、菜谱、点菜单等所有数据，云存储用于存储用户上传的菜品图片；

4. 外部接口层：对接小红书链接解析AI接口、AI图片生成接口，实现菜谱信息自动提炼、菜品图片自动生成。

## 2\.2 核心业务流程图

# 三、页面设计（含交互逻辑）

所有页面需遵循微信小程序设计规范，风格简洁、操作便捷，重点突出checklist交互和状态展示，避免冗余设计；所有表单、按钮需有明确提示，错误操作有反馈（如未填写必填项、链接无效等）。

## 3\.1 基础页面（公共页面）

### 3\.1\.1 登录页（pages/login/login）

- 功能：微信授权登录（仅需获取用户昵称、头像，无需其他隐私信息）；

- 交互：点击“微信授权登录”按钮，调用微信授权接口，登录成功后跳转至家庭选择页（展示用户已加入的所有家庭，支持切换）；若已登录且有默认家庭，直接跳转至首页；

- 元素：微信授权按钮、小程序名称（家庭厨助手）、简单引导语（“登录后即可创建家庭，共享菜谱”）。

### 3\.1\.2 首页（pages/index/index）

- 功能：展示当前家庭信息、待处理点菜单（待买菜、待制作）、常用菜谱入口、添加菜谱入口，支持家庭切换；

- 交互：
        

- 点击“待买菜”点菜单，跳转至买菜页面；

- 点击“待制作”点菜单，跳转至做菜页面；

- 点击“添加菜谱”，跳转至菜谱添加页；

- 点击“常用菜谱”，跳转至菜谱列表页；

- 点击家庭信息（顶部），弹出家庭切换弹窗（展示用户已加入的所有家庭，可选择切换，点击“管理家庭”跳转至家庭管理页）；

- 切换家庭后，页面自动刷新当前家庭的相关数据（菜谱、点菜单等）。

元素：家庭名称（带下拉切换按钮）、成员数量、待处理点菜单卡片（显示菜单名称、创建时间、状态）、添加菜谱按钮、常用菜谱入口。

### 3\.1\.3 家庭管理页（pages/family/family）

- 功能：创建家庭、加入家庭、查看当前家庭成员、退出家庭、查看用户已加入的所有家庭（支持切换）；

- 交互：
        

- 页面顶部显示“我的家庭”列表（用户已加入的所有家庭，标注当前所在家庭），点击其他家庭可直接切换至该家庭；

- 点击“创建家庭”，输入家庭名称，提交后创建家庭（创建者为家庭管理员），创建成功后自动加入该家庭并切换为当前家庭；

- 点击“加入家庭”，输入家庭邀请码，验证通过后加入家庭，可在“我的家庭”列表中切换；

- 当前家庭区域展示家庭成员列表，点击成员头像/昵称，可查看成员信息（仅显示昵称、头像）；

- 管理员可删除当前家庭的成员（弹出确认框），普通成员可点击“退出当前家庭”（弹出确认框），退出后仍保留其他已加入家庭；

- 当前家庭区域顶部显示家庭邀请码（可复制）。

元素：我的家庭列表（标注当前家庭）、当前家庭名称、邀请码（可复制）、创建家庭按钮、加入家庭按钮、成员列表（头像\+昵称）、删除/退出按钮。

## 3\.2 菜谱管理模块页面

### 3\.2\.1 菜谱列表页（pages/recipe/list）

- 功能：展示当前家庭的所有菜谱（每个菜谱显示对应图片），支持搜索、删除、编辑菜谱；

- 交互：
        

- 搜索框输入菜名，实时筛选菜谱；

- 点击菜谱卡片（显示图片、菜名、添加时间），跳转至菜谱详情页；

- 长按菜谱卡片，弹出“编辑”“删除”选项，点击编辑跳转至菜谱编辑页，点击删除弹出确认框（删除后不可恢复）；

- 点击右上角“添加菜谱”，跳转至菜谱添加页。

元素：搜索框、菜谱卡片（显示菜名、菜品图片、添加时间）、添加菜谱按钮。

### 3\.2\.2 菜谱添加页（pages/recipe/add）

- 功能：输入小红书链接，调用AI接口提炼菜谱信息，支持手动修改所有字段；为菜谱添加图片（AI生成或用户上传），支持后续修改；

- 交互：
        

- 输入小红书链接，点击“提炼菜谱”按钮，调用AI接口，加载完成后自动填充所有字段（菜名、食材、调料、备菜步骤、做菜步骤）；

- 菜谱图片设置：点击图片上传区域，弹出选项“AI生成图片”“上传本地图片”；选择AI生成，输入菜名（默认填充提炼的菜名），调用AI图片生成接口，生成后可预览、重新生成；选择上传本地图片，调用微信相册接口，选择图片后可预览、重新上传；

- 所有字段支持手动编辑：食材可添加/删除/修改，调料可修改用量（单位默认g，可手动修改单位），步骤可添加/删除/调整顺序；图片可随时重新设置（AI生成或重新上传）；

- 点击“提交”按钮，验证所有必填字段（菜名、至少1种食材、至少1个步骤、菜品图片），验证通过后保存菜谱至当前家庭，跳转至菜谱列表页；

- 点击“取消”按钮，返回上一页（不保存内容）；

- AI提炼失败（如链接无效、AI接口异常），显示错误提示（“链接无效，请重新输入”“提炼失败，请手动添加”）；AI图片生成失败，提示“图片生成失败，请重新生成或上传本地图片”。

元素：小红书链接输入框、提炼菜谱按钮、菜名输入框、菜品图片上传/AI生成区域（预览图、操作按钮）、食材列表（添加/删除按钮）、调料列表（添加/删除按钮、用量输入框）、备菜步骤列表（添加/删除/排序按钮）、做菜步骤列表（添加/删除/排序按钮）、提交/取消按钮。

### 3\.2\.3 菜谱编辑页（pages/recipe/edit）

- 功能：编辑已有的菜谱，支持修改所有字段（包括重新输入小红书链接提炼信息），支持修改菜品图片（重新AI生成或重新上传）；

- 交互：与菜谱添加页一致，加载时自动填充该菜谱的现有信息（含菜品图片），可修改图片、所有字段，提交后覆盖原有数据，跳转至菜谱列表页。

### 3\.2\.4 菜谱详情页（pages/recipe/detail）

- 功能：展示菜谱的完整信息（菜品图片、菜名、食材、调料及用量、备菜步骤、做菜步骤），支持“点菜”操作；

- 交互：点击“点菜”按钮，弹出确认框（“确认点这道菜吗？”），确认后跳转至点菜备注页面（可添加该菜品的点菜备注），备注完成后添加至当前点菜单（若当前无点菜单，自动创建新的待买菜点菜单），跳转至点菜单列表页；若无需备注，可直接确认添加。

### 3\.2\.5 点菜备注页（pages/recipe/note）

- 功能：为当前点的菜品添加备注信息（如口味偏好、食材替换、制作要求等）；

- 交互：
       

    - 页面显示当前菜品名称、菜品图片，下方为备注输入框（提示“可输入口味、食材替换等备注信息，选填”）；

    - 点击“确认添加”，将菜品及备注信息添加至当前点菜单，跳转至点菜单列表页；

    - 点击“取消”，返回菜谱详情页，不添加该菜品；

    - 备注信息支持换行、输入特殊符号，最大输入长度限制为200字。

- 元素：菜品图片、菜品名称、备注输入框、确认添加/取消按钮。

## 3\.3 点餐模块页面

### 3\.3\.1 点菜单列表页（pages/order/list）

- 功能：展示当前家庭的所有点菜单，按状态分类（待买菜、待制作、已完成）；

- 交互：
        

- 点击“待买菜”点菜单，跳转至买菜页面；

- 点击“待制作”点菜单，跳转至做菜页面；

- 点击“已完成”点菜单，跳转至点菜单详情页（仅查看，不可操作）；

- 点击右上角“新建点菜单”，创建新的待买菜点菜单（可直接点菜）；

- 长按点菜单，弹出“删除”选项（仅管理员和创建者可删除，已完成的点菜单也可删除）。

元素：状态筛选栏（待买菜、待制作、已完成）、点菜单卡片（显示菜单名称、创建时间、状态、包含菜品数量）、新建点菜单按钮。

### 3\.3\.2 点菜页面（pages/order/choose）

- 功能：从家庭菜谱中选择菜品，添加至当前点菜单，可为每个添加的菜品添加备注信息；

- 交互：
        

- 搜索框输入菜名，筛选菜谱（菜谱卡片显示菜品图片、菜名）；

- 点击菜谱卡片右侧“添加”按钮，弹出备注输入框（选填），输入备注后点击“确认”，添加至当前点菜单（显示已添加标记及备注预览）；无需备注可直接点击“跳过备注”完成添加；

- 顶部显示当前点菜单名称（可修改）、已添加菜品数量；

- 点击“完成点菜”按钮，跳转至点菜单列表页（点菜单状态为待买菜）；

- 点击“已添加菜品”区域，可删除已添加的菜品，删除时一并删除该菜品的备注信息。

元素：点菜单名称输入框、搜索框、菜谱列表（带添加/已添加按钮、菜品图片）、已添加菜品区域（带删除按钮、备注预览）、完成点菜按钮。

### 3\.3\.3 点菜单详情页（pages/order/detail）

- 功能：查看点菜单的完整信息（菜单名称、创建者、创建时间、包含菜品及备注、当前状态）；

- 交互：根据状态显示对应操作按钮（待买菜显示“去买菜”，待制作显示“去做菜”，已完成无按钮），点击按钮跳转至对应操作页面；点击菜品可查看该菜品的完整信息及备注。

## 3\.4 买菜模块页面（pages/shopping/shopping）

- 功能：展示当前点菜单所有菜品的食材和调料，以checklist形式呈现，支持标记已购买；可查看各菜品的备注信息；

- 交互：


- 食材和调料按菜品分类展示，每个菜品旁标注备注信息（无备注则不显示），每个条目带复选框，点击复选框标记为“已购买”（勾选后变灰，不可取消）；

- 顶部显示点菜单名称、已购买/总条目数量；

- 所有条目勾选完成后，自动弹出确认框（“确认已完成所有采购吗？”），确认后点菜单状态变为“待制作”，跳转至点菜单列表页；

- 点击“返回”按钮，返回点菜单详情页（保存当前勾选状态）；

- 支持手动添加额外采购项（如临时需要的食材/调料），可删除。

元素：点菜单名称、采购进度（已购买/总）、食材/调料checklist（按菜品分组，带复选框、名称、用量，菜品旁标注备注）、添加额外采购项按钮、返回按钮。

## 3\.5 做菜模块页面（pages/cooking/cooking）

- 功能：展示当前点菜单所有菜品的备菜步骤和做菜步骤，以checklist形式呈现，支持标记已完成；可查看各菜品的备注信息；

- 交互：
        

- 按菜品分类展示，每个菜品旁标注备注信息（无备注则不显示），先显示该菜品的备菜步骤，再显示做菜步骤，每个步骤带复选框，点击复选框标记为“已完成”（勾选后变灰，不可取消）；

- 顶部显示点菜单名称、已完成/总步骤数量；

- 所有步骤勾选完成后，自动弹出确认框（“确认已完成所有制作步骤吗？”），确认后点菜单状态变为“已完成”，跳转至点菜单列表页；

- 点击“返回”按钮，返回点菜单详情页（保存当前勾选状态）；

- 步骤支持展开/折叠（默认展开，点击菜品名称可折叠该菜品的所有步骤）。

元素：点菜单名称、制作进度（已完成/总）、菜品列表（带展开/折叠按钮，包含备菜步骤、做菜步骤checklist，菜品旁标注备注）、返回按钮。

# 四、数据结构设计（云数据库集合）

所有集合需设置对应权限，确保数据隔离（仅当前家庭成员可访问家庭相关数据，用户仅可访问自己创建/加入的家庭数据）；主键使用云数据库自动生成的\_id，所有时间字段使用Date类型（默认当前时间）；菜品图片存储在云存储，数据库中仅存储图片的云存储地址。

## 4\.1 用户集合（users）

|字段名|类型|必填|说明|
|---|---|---|---|
|\_id|String|是|用户唯一标识（微信openid）|
|nickName|String|是|用户昵称（微信授权获取）|
|avatarUrl|String|是|用户头像（微信授权获取）|
|familyIds|Array|是|用户已加入的所有家庭ID列表（元素为家庭\_id，未加入家庭则为空数组）|
|currentFamilyId|String|否|当前所在家庭ID（未加入家庭则为null，加入多个家庭时，存储当前切换的家庭ID）|
|familyRoles|Object|是|用户在各家庭中的角色，key为家庭\_id，value为角色（admin：管理员，member：普通成员），未加入家庭则为空对象|
|createTime|Date|是|用户注册时间（默认当前时间）|

## 4\.2 家庭集合（families）

|字段名|类型|必填|说明|
|---|---|---|---|
|\_id|String|是|家庭唯一标识|
|familyName|String|是|家庭名称（用户自定义）|
|inviteCode|String|是|家庭邀请码（6位随机字符串，唯一）|
|adminId|String|是|家庭管理员ID（创建者的openid）|
|memberIds|Array|是|家庭成员ID列表（包含管理员，元素为openid）|
|createTime|Date|是|家庭创建时间（默认当前时间）|

## 4\.3 菜谱集合（recipes）

|字段名|类型|必填|说明|
|---|---|---|---|
|\_id|String|是|菜谱唯一标识|
|familyId|String|是|所属家庭ID（关联families集合）|
|recipeName|String|是|菜名|
|recipeImg|String|是|菜品图片云存储地址（AI生成或用户上传，不可为空）|
|xiaohongshuUrl|String|否|小红书链接（用户添加时输入）|
|ingredients|Array|是|食材列表，每个元素为对象：\{name: String（食材名称）, amount: String（用量，可选）\}|
|seasonings|Array|是|调料列表，每个元素为对象：\{name: String（调料名称）, amount: String（用量，如“5g”）\}|
|prepareSteps|Array|是|备菜步骤列表，每个元素为String（步骤描述）|
|cookingSteps|Array|是|做菜步骤列表，每个元素为String（步骤描述）|
|creatorId|String|是|创建者ID（关联users集合）|
|createTime|Date|是|菜谱创建时间（默认当前时间）|
|updateTime|Date|是|菜谱更新时间（默认创建时间，修改后更新，包括图片修改）|

## 4\.4 点菜单集合（orders）

|字段名|类型|必填|说明|
|---|---|---|---|
|\_id|String|是|点菜单唯一标识|
|familyId|String|是|所属家庭ID（关联families集合）|
|orderName|String|是|点菜单名称（默认“XX的点菜单”，可修改）|
|status|String|是|点菜单状态：pending\_shopping（待买菜）、pending\_cooking（待制作）、completed（已完成），默认pending\_shopping|
|recipes|Array|是|包含的菜谱信息列表，每个元素为对象：\{recipeId: String（菜谱ID）, note: String（点菜备注，可选，无备注则为空字符串）\}|
|creatorId|String|是||

> （注：文档部分内容可能由 AI 生成）
